  \documentclass[tikz]{standalone}
\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\begin{document}
\pagestyle{empty}


% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=blue!20, 
    text width=4.5em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!20, 
    text width=5em, text centered, rounded corners, minimum height=4em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [draw, ellipse,fill=red!20, node distance=3cm,
    minimum height=2em]
    
\begin{tikzpicture}[node distance = 2cm, auto]
    % Place nodes
   \node [block] (init) {Scenario Selection};
    \node [block, below of=init] (receive) {Fishstate Receives Scenario};
   \node [block, below of=receive] (pressplay) {Press \raisebox{-1pt}{\includegraphics[height=9pt]{play}}};
   \node [block, below of=pressplay] (fishstart) {Fishstate start()};
   \node [block, below of=fishstart] (scenariostart) {Scenario start()};
   \node [block, right of=scenariostart,node distance = 3cm] (species) {Build Species};
   \node [block, below of=species] (map) {Build Map};
   \node [block, below of=map] (markets) {Build Markets};
   \node [block, below of=markets] (fishers) {Build Fishers};
   \node [block, left of=fishers, node distance = 3cm] (endstart) {Scenario start() ends};
   \node [block, below of=endstart] (agentsstart) {Call start() on all agents};
   \node [block, below of=agentsstart] (endfishstate) {Fishstate start() ends};
   \node [block, below of=endfishstate] (runrun) {Model Runs};
\path [line] (init) -- (receive);
\path [line] (receive) -- (pressplay);
\path [line] (pressplay) -- (fishstart);
\path [line] (fishstart) -- (scenariostart);
\path [line] (scenariostart) -- (species);
\path [line] (species) -- (map);
\path [line] (map) -- (markets);
\path [line] (markets) -- (fishers);
\path [line] (fishers) -- (endstart);
\path [line] (endstart) -- (agentsstart);
\path [line] (agentsstart) -- (endfishstate);
\path [line] (endfishstate) -- (runrun);



 %   \node [cloud, left of=init] (expert) {expert};
%    \node [cloud, right of=init] (system) {system};
 %   \node [block, below of=init] (identify) {identify candidate models};
%    \node [block, below of=identify] (evaluate) {evaluate candidate models};
%    \node [block, left of=evaluate, node distance=3cm] (update) {update model};
%    \node [decision, below of=evaluate] (decide) {is best candidate better?};
%    \node [block, below of=decide, node distance=3cm] (stop) {stop};
    % Draw edges
   % \path [line] (init) -- (identify);
%    \path [line] (identify) -- (evaluate);
%    \path [line] (evaluate) -- (decide);
%    \path [line] (decide) -| node [near start] {yes} (update);
%    \path [line] (update) |- (identify);
%    \path [line] (decide) -- node {no}(stop);
%    \path [line,dashed] (expert) -- (init);
%    \path [line,dashed] (system) -- (init);
%    \path [line,dashed] (system) |- (evaluate);
\end{tikzpicture}
\end{document}